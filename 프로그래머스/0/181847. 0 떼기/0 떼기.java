class Solution {
    public String solution(String n_str) {
        // int cnt = 0;
        // for(int i=0; i<n_str.length(); i++) {
        //     if (("" + n_str.charAt(i)).equals("0") == false) {
        //         break;
        //     }
        //     cnt += 1;
        // }
        // return n_str.substring(cnt);
        return String.valueOf(Integer.parseInt(n_str));
        }
    
}